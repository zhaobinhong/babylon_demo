```$xslt
alpha
alpha: boolean
继承自EngineOptions.alpha
---------------------------------

antialias
antialias: boolean
继承自EngineOptions.antialias
---------------------------------

audioEngine
audioEngine: boolean
定义是否也应初始化webaudio
---------------------------------

autoEnableWebVR
autoEnableWebVR: boolean
定义是否应自动启用webvr
---------------------------------

depth
depth: boolean
继承自EngineOptions.depth
---------------------------------

desynchronized
desynchronized: boolean
继承自EngineOptions.desynchronized
---------------------------------

deterministicLockstep
deterministicLockstep: boolean
定义动画是否应使用确定性锁定步骤运行
---------------------------------

disableWebGL2Support
disableWebGL2Support: boolean
定义是否应关闭webgl2（即使受支持）
---------------------------------

doNotHandleContextLost
doNotHandleContextLost: boolean
定义引擎应该忽略上下文丢失事件如果此事件为true时发生此事件，则必须重新加载页面以还原呈现
---------------------------------

doNotHandleTouchAction
doNotHandleTouchAction: boolean
定义引擎应忽略修改触摸动作属性和样式的问题。如果无法处理，则可能需要将其设置在侧面以实现预期的触摸设备行为。
---------------------------------

failIfMajorPerformanceCaveat
failIfMajorPerformanceCaveat: boolean
继承自EngineOptions.failIfMajorPerformanceCaveat
---------------------------------

limitDeviceRatio
limitDeviceRatio: number
定义引擎是否不应超过指定的设备比率
---------------------------------

lockstepMaxSteps
lockstepMaxSteps: number
定义确定性锁步模式下使用的最大步数
---------------------------------

powerPreference
powerPreference: WebGLPowerPreference
继承自EngineOptions.powerPreference
---------------------------------

premultipliedAlpha
premultipliedAlpha: boolean
继承自EngineOptions.premultipliedAlpha
---------------------------------

preserveDrawingBuffer
preserveDrawingBuffer: boolean
继承自EngineOptions.preserveDrawingBuffer
---------------------------------

stencil
stencil: boolean
继承自EngineOptions.stencil
---------------------------------

timeStep
timeStep: number
定义每个确定性锁定步骤之间的秒数
---------------------------------

useHighPrecisionFloats
useHighPrecisionFloats: boolean
定义引擎应编译具有高精度浮点的着色器（如果支持）。默认为真
---------------------------------

xrCompatible
xrCompatible: boolean
使canvas XR兼容XR会话


```
